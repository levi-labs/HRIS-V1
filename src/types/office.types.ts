export interface OfficeRequest {
    name: string;
    latitude: number;
    longitude: number;
}

export interface OfficeRespose {
    id: number;
    name: string;
    latitude: number;
    longitude: number;
}